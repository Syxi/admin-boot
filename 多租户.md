
# 多租户功能详细设计方案

根据对您系统的深入分析，我为您制定了一个完整的多租户功能实现方案。该方案遵循您现有的系统架构和认证流程，确保不影响现有功能的同时实现多租户能力。

## 1. 系统现状分析

### 1.1 认证流程
您的系统采用标准的Spring Security + JWT Token认证流程：
1. 用户通过 `/auth/login` 接口登录
2. 系统通过 [SysUserDetailsService](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SysUserDetailsService.java#L19-L43) 加载用户信息
3. 认证成功后，[JwtTokenServiceImpl](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/service/impl/JwtTokenServiceImpl.java#L36-L297) 生成包含用户信息的JWT Token
4. 前端后续请求携带Token，由 [JwtValidationFilter](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/filter/JwtValidationFilter.java#L32-L119) 验证并解析Token
5. [SecurityUtils](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SecurityUtils.java#L22-L146) 提供获取当前用户信息的工具方法

### 1.2 现有多租户组件
系统已部分实现了多租户功能：
- [SysTenant](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/entity/SysTenant.java) 租户实体和管理接口
- [SysTenantUser](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/entity/SysTenantUser.java) 租户用户关联关系
- [MyTenantLineHandler](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/handler/MyTenantLineHandler.java#L15-L40) MyBatis Plus多租户插件处理器
- [MybatisPlusConfig](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/config/MybatisPlusConfig.java#L25-L68) 中已启用多租户插件

## 2. 多租户功能设计方案

### 2.1 整体架构
采用"数据库级别隔离"方案，在同一数据库中通过 `tenant_id` 字段区分不同租户的数据。

### 2.2 核心实现思路
1. 在用户实体 [SysUser](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/entity/SysUser.java#L21-L122) 中添加 `tenant_id` 字段
2. 在JWT Token中携带租户ID信息
3. 通过MyBatis Plus多租户插件自动为SQL添加租户过滤条件
4. 在数据自动填充时自动设置租户ID

## 3. 详细实施步骤

### 3.1 修复认证流程中的租户ID处理

#### 3.1.1 修复SecurityUtils中的租户ID获取方法
当前 [SecurityUtils.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SecurityUtils.java) 中的 `getTenantId()` 方法存在错误，它返回的是用户ID而非租户ID：

```java
// 错误实现
public static Long getTenantId() {return getCurrentUser().map(SysUserDetails::getUserId).orElse(null);}

// 正确实现
public static Long getTenantId() {return getCurrentUser().map(SysUserDetails::getTenantId).orElse(null);}
```

#### 3.1.2 完善SysUserDetails中的租户ID处理
确保 [SysUserDetails](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SysUserDetails.java#L27-L128) 正确处理租户ID，并提供默认构造函数。

### 3.2 完善JWT Token中的租户信息处理

#### 3.2.1 在Token生成时添加租户ID
修改 [JwtTokenServiceImpl](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/service/impl/JwtTokenServiceImpl.java#L36-L297) 的 `createToken` 方法，在claims中添加租户ID：

```java
// 添加租户ID（如果存在）
Long tenantId = userDetails.getTenantId();
if (tenantId != null) {
    claims.put(JwtClaimConstants.TENANT_ID, tenantId);
}
```

#### 3.2.2 在Token解析时正确还原租户ID
修改 [JwtTokenServiceImpl](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/service/impl/JwtTokenServiceImpl.java#L36-L297) 的 `parseToken` 方法，正确从claims中获取租户ID并设置到 [UserAuthInfo](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/dto/UserAuthInfo.java#L13-L42) 和 [SysUserDetails](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SysUserDetails.java#L27-L128) 中。

### 3.3 完善数据库实体和Mapper

#### 3.3.1 确保SysUser实体包含tenantId字段
确认 [SysUser.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/entity/SysUser.java) 实体类中包含 `tenantId` 字段，并在Mapper XML中正确映射。

#### 3.3.2 更新Mapper XML文件
确保 [SysUserMapper.xml](file:///f:/workspace/admin-boot/src/main/resources/mapper/SysUserMapper.xml) 中包含 `tenant_id` 字段的映射。

### 3.4 完善数据自动填充机制

#### 3.4.1 启用MyMetaObjectHandler中的租户ID自动填充
取消 [MyMetaObjectHandler.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/handler/MyMetaObjectHandler.java) 中租户ID自动填充的注释，确保在插入数据时自动填充租户ID：

```java
// 自动填充tenantId字段
Long currentTenantId = SecurityUtils.getTenantId();
if (metaObject.hasSetter(TENANT_ID) && currentTenantId != null) {
    this.strictInsertFill(metaObject, TENANT_ID, Long.class, currentTenantId);
}
```

### 3.5 完善MyBatis Plus多租户插件配置

#### 3.5.1 改进MyTenantLineHandler的租户ID获取逻辑
修改 [MyTenantLineHandler.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/handler/MyTenantLineHandler.java) 中的 `getTenantId()` 方法，确保在无法获取租户ID时不抛出异常：

```java
@Override
public Expression getTenantId() {
    Long tenantId = SecurityUtils.getTenantId();
    // 如果无法获取租户ID，返回默认值0，避免影响非多租户场景
    if (tenantId == null) {
        return new LongValue(0L);
    }
    return new LongValue(tenantId);
}
```

### 3.6 完善用户认证信息流转

#### 3.6.1 在UserAuthInfo中添加租户ID
确保 [UserAuthInfo.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/dto/UserAuthInfo.java) DTO中包含 `tenantId` 字段。

#### 3.6.2 在SysUserServiceImpl中正确设置租户ID
修改 [SysUserServiceImpl.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/service/impl/SysUserServiceImpl.java) 中的 `convertToUserAuthInfo` 方法，确保从 [SysUser](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/entity/SysUser.java) 实体正确获取租户ID并设置到 [UserAuthInfo](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/dto/UserAuthInfo.java) 中。

### 3.7 完善日志记录中的租户信息

#### 3.7.1 在用户登录日志中添加租户ID
修改 [UserLoginLogServiceImpl.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/service/impl/UserLoginLogServiceImpl.java) 中的 `saveLoginLog` 方法，在保存登录日志时记录租户ID。

#### 3.7.2 在用户操作日志中添加租户ID
修改 [UserOperationLogServiceImpl.java](file:///f:/workspace/admin-boot/src/main/java/com/admin/module/system/service/impl/UserOperationLogServiceImpl.java) 中的 `saveUserOperationLog` 方法，在保存操作日志时记录租户ID。

## 4. 异常处理和边界情况

### 4.1 无租户用户处理
对于系统中可能存在的没有分配租户的用户（如超级管理员），需要确保：
1. 在Token中不包含租户ID信息
2. MyBatis Plus多租户插件能正确处理这种情况
3. 不会影响这类用户的正常登录和操作

### 4.2 租户数据隔离验证
确保多租户插件正确地为所有需要租户隔离的表添加过滤条件，同时排除不需要隔离的表（如租户表本身、字典表等）。

## 5. 测试验证方案

### 5.1 单元测试
1. 测试 [SecurityUtils.getTenantId()](file:///f:/workspace/admin-boot/src/main/java/com/admin/common/security/SecurityUtils.java#L52-L52) 方法正确性
2. 测试JWT Token生成和解析时租户ID的正确处理
3. 测试MyBatis Plus多租户插件的过滤逻辑

### 5.2 集成测试
1. 测试不同租户用户登录后只能访问自己租户的数据
2. 测试无租户用户（如超级管理员）的特殊处理
3. 测试跨租户数据访问的隔离性

这个设计方案充分考虑了您现有系统的架构特点，通过最小化的改动实现完整的多租户功能，既保证了功能的完整性，又最大程度降低了对现有系统的影响。