# 数据权限功能使用说明

## 1. 功能概述

1**无侵入方式**：使用 `@DataPermission` 注解自动应用权限

## 2. 无侵入式数据权限使用方法

### 2.1 基本使用

在Service方法上添加 `@DataPermission` 注解：

```java
@DataPermission
public IPage<UserVO> selectUserPage(UserQuery userQuery) {
    LambdaQueryWrapper<SysUser> queryWrapper = new LambdaQueryWrapper<>();
    // 构建查询条件...
    
    // 无需手动调用DataPermissionHelper，权限会自动应用
    IPage<SysUser> pageData = this.page(new Page<>(userQuery.getPage(), userQuery.getLimit()), queryWrapper);
    // ...
}
```

### 2.2 自定义字段名

可以通过注解参数自定义字段名：

```java
@DataPermission(deptField = "department_id", userField = "creator_id")
public List<Order> selectOrders() {
    LambdaQueryWrapper<Order> queryWrapper = new LambdaQueryWrapper<>();
    // 构建查询条件...
    
    List<Order> orders = this.list(queryWrapper);
    // ...
}
```


## 4. 权限类型说明

系统支持以下几种数据权限类型：

1. **全部数据权限** (ALL): 可查看所有数据
2. **部门及子部门数据权限** (DEPT_AND_CHILDREN): 可查看所在部门及其子部门的数据
3. **本部门数据权限** (DEPT): 可查看所在部门的数据
4. **本人数据权限** (CREATE_USER): 只能查看自己创建的数据

## 5. 实现原理

### 5.1 AOP切面
- `DataPermissionAspect` 切面拦截带有 `@DataPermission` 注解的方法
- 根据当前用户的数据权限范围，设置相应的权限上下文

### 5.2 MyBatis拦截器
- `DataPermissionInterceptor` 拦截SQL执行
- 自动分析SQL语句并添加相应的数据权限条件
- 避免重复添加相同字段的条件

### 5.3 上下文传递
- 使用 `DataPermissionContext` 在线程内传递权限信息
- 结合ThreadLocal实现线程安全的数据传递

## 6. 注意事项

1. **注解位置**：`@DataPermission` 注解必须放在Service层的方法上，不能放在Mapper层
2. **字段命名**：确保数据库表中的字段名与注解中配置的字段名一致
3. **性能考虑**：对于复杂的查询，建议使用传统方式以便更好地控制权限逻辑
4. **权限检查**：系统会自动跳过管理员用户的数据权限检查